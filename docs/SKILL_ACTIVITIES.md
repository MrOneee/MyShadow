# Skill 活动运行能力

机器人继续每三秒检查数据库变化；模型推理交给最多两个工作线程，每个线程使用自己的 SQLite 连接、AI 客户端和 Bot 实例。同群同时只有一个任务，不同群可并行推理。桌面切群、文字发送、表情搜索到发送的整个操作链持有同一把可重入锁，不会交叉点击。

## 使用海龟汤

- `@影 来一局海龟汤`：从已审核且本群未玩过的题中开局。
- `@影 找几个高质量海龟汤`：展示不含汤底的候选，发起者选题。
- `@影 联网搜一个海龟汤`：实际搜索、整理、独立审核及专项可玩性检查；有缺陷修订一次再审核，不把未通过审核的题拿来主持。
- 开局后直接提问，不需要 @。`进度`、`提示`、`重发汤面`对玩家开放。
- 发起人或配置中的管理员可以`暂停游戏`、`继续游戏`、`结束游戏`、`换一锅`。
- 进度由已核验的玩家证据计算，提示不涨进度。达到85%且核心因果全部破解才自动揭晓；主动结束会明确说明结束而不是宣称破解。
- 三分钟没有游戏进展时提醒一次；不连续催促。暂停、结束或新事件会使旧定时事件失效。

## 宿主与能力包边界

`activity_runtime/`管理注册、结构化调用、会话、事件、内容候选、权限、角色资料投影、定时事件和出站队列。它没有海龟汤规则。

`skills/turtle_soup/`拥有题库、题目校验、选题审核、裁判提示词、证据复核、计分、提示、暂停续局、揭晓和复盘。`SKILL.md`是说明入口，`handler.py`是执行入口。包代码是部署时审核的固定代码，聊天模型不能生成或执行任意脚本。

一个新能力包声明：

- `manifest.json`：id、接口版本、状态版本、入口、能力权限、模型角色可访问的私有字段、动作允许的角色。
- `Skill.matches(text)`：入口匹配；`handle(context, public, private, event)`：返回数据形式的 `Turn`。
- `validate_state(public, private)`：专用状态约束；`can_publish(context, turn)`：受控资料发布的场景条件。
- `SKILL.md`、实际需要的提示词、数据、脚本和测试。

宿主绑定真实群和发送者，包不能从模型参数改变目标群。状态包含公开和私有部分，模型调用必须声明角色。普通表达角色不接收私有汤底；裁判的内部解释不直接发群。代码插件本身是可信代码，此接口不宣称提供操作系统沙箱。

## 持久化与失败语义

消息首先进入持久化活动收件箱，按消息时间与原始顺序号处理。同一群一个活动会话；状态版本更新、事件记录和待发送回复在一个事务中提交。重复消息不会产生第二份进度或第二条出站消息。

推理中断可以重做，因为尚未产生外部效果；进入发送后的不确定消息不会自动重发。桌面发送仍依赖实际客户端及数据库送达确认，无法承诺任意故障下的网络级 exactly-once。发送状态保存在原有 replies 表，并通过 activity_outbox 关联会话和版本。

重启恢复活动状态，不从滚动摘要重建汤底。状态版本不兼容时明确停止该活动，部署前需提供并验证数据迁移，不静默丢弃存档。

## 配置

`bot.json`新增 `group_workers: 2`，以及 `activities: {"enabled": true, "skills": ["turtle_soup"], "model": "deepseek-v4-pro"}`。管理员沿用现有 scheduler.admin_id；普通成员可以开局，只有本局发起人和管理员能结束或换题。

## 诊断与回放

日志事件包括 `activity_model`、`activity_transition`、`activity_error`、`group_worker_error`，关联会话、输入和状态版本。公开日志不输出完整汤底或凭据；详细判定保存在受保护的状态数据库事件记录中。

本地运维命令（不发送消息）：

```sh
python -m activity_runtime list
python -m activity_runtime audit --session INTERNAL_SESSION_ID
python -m activity_runtime export --session INTERNAL_SESSION_ID
python -m unittest -q
ACTIVITY_MODEL=deepseek-v4-pro PYTHONPATH=. python skills/turtle_soup/tests/live_replay.py
```

`audit`以事件重新检查状态快照及事务出站记录。`export`只导出当时公开的问答和状态。内部标识供运维使用，不在玩家回复中展示。真实模型回放使用内存数据库和预览模式，不导入微信发送器。

## 题目来源与编排

内置题按固定版本主持，全部标记出处和改编说明；不声称经典谜题是原创或题面逻辑上具有唯一解释。题库不是实时搜索的伪装。挑题时，冷僻知识、措辞说谎、重复题面计分及直接泄底的提示均列为问题。

来源包含 Brainteasers 的电梯题、Paul Sloane 的酒吧题、ThePuzzleLabs 的金鱼题、TrainThinking 的雪人题以及 Braingle 的午夜电话题。来源链接在各题数据与揭晓消息中保留。摄影题因技术知识门槛不适合作为本次默认体验，已移除。

## 本次验收

Linux 环境下 305 项自动化测试通过；五道题共 30 轮真实模型对局通过，覆盖错误猜测、多人接力、拒绝提前泄底及完整揭晓。两个真实群的工作线程联调使用独立预览数据库，未向群发送测试消息。题目复核采用推理模式，结合公开题面解析代词，只接收候选事实、不传入完整汤底；保留内部判断依据供回放检查。

外部模型及搜索服务仍可能超时或判断失误；已有固定题目、结构校验、证据复核、纠错入口和失败状态，但测试通过不代表任意自然语言都有确定性保证。

活动模型通过 activities.model 独立配置；本次选择现有接口上的 DeepSeek V4 Pro，普通聊天模型配置保持原值。省略该字段则沿用普通聊天模型。选题与证据核验启用推理，网络选题先发一条处理进度消息。

最终对局验收中，单条游戏消息的模型处理时间中位数为7.23秒，最大23.13秒（不包含桌面送达）；联网选题还需要检索与内容审核，可能耗时数分钟。
