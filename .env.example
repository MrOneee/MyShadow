# Generate a separate random password for the browser desktop.
# Do not reuse the server SSH password or put a model API key here.
WECHAT_WEB_PASSWORD=
WECHAT_WEB_USER=weixin
# Client identity after logging in. These are not account/password login fields.
WECHAT_NAME=
WECHAT_BOT_ID=
# Keep preview while verifying the deployment; send enables real group replies.
BOT_MODE=preview
