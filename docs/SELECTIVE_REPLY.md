# 群聊参与决策

仍由3秒轮询驱动，仅对selective_reply.groups中的群启用；其他群保持真实@入口。

`selective_reply.decision.enabled=true`使用新的对话决策；关闭该子开关回到原保守策略。真实@、明确叫影/道长、已核验引用保留直接响应。其余文本按对话批次判断，不再要求问号、求助词，寒暄和玩笑也可以参与。

决策读取本群最近最多24条消息（每条最多300字、最多6小时），匿名区分发言人，提供最近已确认的机器人问答、同一发言者近期互动次数、是否正在接续对话。选择silent（不说）、wait（等10秒再判断一次）、brief（简短接话）、answer（认真回答）或initiate（主动延续话题）。模型没有执行工具；决定参与后才进入回复生成。

- 同一发言者2秒内最多3条合并。消息先等1.5秒安静期，最多5秒；每群决策至少间隔8秒。
- 默认每群每小时最多60次判断、12次无明确称呼的参与（包括主动发起）；额度在决策时占用，失败也不刷重试。明确称呼和真实@不受此额度限制。
- 上一次确认回复后通常等30秒，同一发言者在120秒内继续互动时缩至8秒；最近12条中已有4条来自影时收敛。
- 23:00到08:00（北京时间）不做无明确称呼的参与，直接请求和已有定时任务不受影响。
- 主动发起默认每天最多1次（proactive_daily_limit可设0关闭），仅在距离最近人类消息5分钟到2小时、有近期具体话题时考虑。同一话题锚点空闲判断一次。已经发言而没人回应时不追加。不凭空编造新闻、不因为冷场而硬打招呼，私人进展必须有邀请才追问。
- 主动发起第一版仅发文字，可使用已有搜索/天气工具，不开放任务管理、记忆控制和表情工具。不是每天保证发送。
- 发出前读取最新微信快照，发现锚点之后又有人发言就取消旧的可选回复。主动消息也使用原始话题锚点比较，不能因排队时间较新而忽略插入的新消息。
- 模型异常默认沉默。同一轮决策先持久化再调用API，重启不重复判断。主动发起用独立发送记录、与原消息锚点关联，不能冒充某个群友新发的指令。

决策状态保存在participation_turns（保留7天），发送方向和锚点在participation_plans，消息窗口最多6小时。不新增线程、不常驻缓存完整微信数据库。日志participation_decision记录阶段和动作；“更像群友”仍依赖模型判断，等待和发言限额由代码执行。

## 表情

工具固定先看爱心收藏。有收藏时在可见前5张中随机取一张，收藏为空再按短关键词公开搜索；公开结果同样在前5张随机选一张。没有视觉模型调用，不分析图意，不做收藏语义匹配。仅用本地像素占用检测排除空白格、加载占位和加号按钮，不识别图片内容。布局或空白检测异常时停止；不能保证所有未知客户端布局都能识别。

公开搜索仍限制短词（通常2到4字、最多6字），长句先由文本模型提炼；空结果最多换词一次。一次工具调用最多检查收藏一次、公开搜索两次。返回只有一个随机候选，Agent直接使用它发送，不再让用户或模型挑图。群绑定、90秒有效期、发送前面板校验和发送回执确认保留。
